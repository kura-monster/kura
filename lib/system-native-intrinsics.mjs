// SPDX-License-Identifier: MIT OR Apache-2.0
import {llvmType,parseSystemType} from './system.mjs';
import {fail,isPointer} from './system-native-common.mjs';

function pathName(expression){if(expression.kind==='Identifier')return expression.name;if(expression.kind==='MemberExpression')return`${pathName(expression.object)}.${expression.property}`;return null;}
function fromAddress(emitter,expression){emitter.requireUnsafe(expression.token,'Raw pointer creation');if(expression.typeArguments.length!==1||expression.args.length!==1)fail('pointer.from_address<T>(address) expected.',expression.token,emitter.compiler.context);const type=emitter.compiler.resolveType(expression.typeArguments[0],expression.token),address=emitter.cast(emitter.compileExpression(expression.args[0]),parseSystemType('usize',{target:emitter.compiler.target.triple}),expression.token),pointer=emitter.temp('ptr');emitter.emit(`${pointer} = inttoptr ${llvmType(address.type,{target:emitter.compiler.target.triple})} ${address.value} to ptr`);return{type:{kind:'pointer',mutable:true,pointee:type},value:pointer};}
function memoryRead(emitter,expression,volatileFlag){emitter.requireUnsafe(expression.token,'Raw memory read');if(expression.typeArguments.length!==1||expression.args.length!==1)fail('memory.read<T>(address) expected.',expression.token,emitter.compiler.context);const type=emitter.compiler.resolveType(expression.typeArguments[0],expression.token),address=emitter.cast(emitter.compileExpression(expression.args[0]),parseSystemType('usize',{target:emitter.compiler.target.triple}),expression.token),pointer=emitter.temp('ptr'),value=emitter.temp('read');emitter.emit(`${pointer} = inttoptr i64 ${address.value} to ptr`);emitter.emit(`${value} = load${volatileFlag?' volatile':''} ${llvmType(type,{target:emitter.compiler.target.triple})}, ptr ${pointer}, align ${emitter.compiler.alignOf(type)}`);return{type,value};}
function memoryWrite(emitter,expression,volatileFlag){emitter.requireUnsafe(expression.token,'Raw memory write');if(expression.typeArguments.length!==1||expression.args.length!==2)fail('memory.write<T>(address,value) expected.',expression.token,emitter.compiler.context);const type=emitter.compiler.resolveType(expression.typeArguments[0],expression.token),address=emitter.cast(emitter.compileExpression(expression.args[0]),parseSystemType('usize',{target:emitter.compiler.target.triple}),expression.token),value=emitter.cast(emitter.compileExpression(expression.args[1]),type,expression.token),pointer=emitter.temp('ptr');emitter.emit(`${pointer} = inttoptr i64 ${address.value} to ptr`);emitter.emit(`store${volatileFlag?' volatile':''} ${llvmType(type,{target:emitter.compiler.target.triple})} ${value.value}, ptr ${pointer}, align ${emitter.compiler.alignOf(type)}`);return{type:parseSystemType('void',{target:emitter.compiler.target.triple}),value:null};}
function pointerMethod(emitter,expression,method){const pointer=emitter.compileExpression(expression.callee.object);if(!isPointer(pointer.type))fail('Pointer method requires pointer.',expression.token,emitter.compiler.context);emitter.requireUnsafe(expression.token,`Pointer ${method}`);if(method==='offset'){if(expression.args.length!==1)fail('.offset(index) requires one argument.',expression.token,emitter.compiler.context);const index=emitter.cast(emitter.compileExpression(expression.args[0]),parseSystemType('isize',{target:emitter.compiler.target.triple}),expression.token),result=emitter.temp('offset');emitter.emit(`${result} = getelementptr ${llvmType(pointer.type.pointee,{target:emitter.compiler.target.triple})}, ptr ${pointer.value}, i64 ${index.value}`);return{type:pointer.type,value:result};}if(method==='read'||method==='volatile_read'){if(expression.args.length!==0)fail(`.${method}() takes no arguments.`,expression.token,emitter.compiler.context);const result=emitter.temp('read');emitter.emit(`${result} = load${method==='volatile_read'?' volatile':''} ${llvmType(pointer.type.pointee,{target:emitter.compiler.target.triple})}, ptr ${pointer.value}, align ${emitter.compiler.alignOf(pointer.type.pointee)}`);return{type:pointer.type.pointee,value:result};}if(!pointer.type.mutable)fail('Cannot write through const pointer.',expression.token,emitter.compiler.context);if(expression.args.length!==1)fail(`.${method}(value) requires one argument.`,expression.token,emitter.compiler.context);const value=emitter.cast(emitter.compileExpression(expression.args[0]),pointer.type.pointee,expression.token);emitter.emit(`store${method==='volatile_write'?' volatile':''} ${llvmType(pointer.type.pointee,{target:emitter.compiler.target.triple})} ${value.value}, ptr ${pointer.value}, align ${emitter.compiler.alignOf(pointer.type.pointee)}`);return{type:parseSystemType('void',{target:emitter.compiler.target.triple}),value:null};}
function cpuInstruction(emitter,expression,assembly,noreturn){emitter.requireUnsafe(expression.token,`CPU instruction '${assembly}'`);if(expression.args.length!==0)fail(`CPU instruction '${assembly}' takes no arguments.`,expression.token,emitter.compiler.context);emitter.emit(`call void asm sideeffect "${assembly}", ""()`);if(noreturn){emitter.emit('unreachable');emitter.terminated=true;return{type:parseSystemType('never',{target:emitter.compiler.target.triple}),value:null};}return{type:parseSystemType('void',{target:emitter.compiler.target.triple}),value:null};}
export function compileSystemIntrinsic(emitter,expression){const path=pathName(expression.callee);if(path==='pointer.from_address')return fromAddress(emitter,expression);if(path==='memory.read')return memoryRead(emitter,expression,false);if(path==='memory.volatile_read')return memoryRead(emitter,expression,true);if(path==='memory.write')return memoryWrite(emitter,expression,false);if(path==='memory.volatile_write')return memoryWrite(emitter,expression,true);const cpu={"cpu.halt":['hlt',true],"cpu.pause":['pause',false],"cpu.disable_interrupts":['cli',false],"cpu.enable_interrupts":['sti',false],"cpu.breakpoint":['int3',false]}[path];if(cpu)return cpuInstruction(emitter,expression,...cpu);if(expression.callee.kind==='MemberExpression'&&['read','volatile_read','write','volatile_write','offset'].includes(expression.callee.property))return pointerMethod(emitter,expression,expression.callee.property);return null;}
